# Hands on History
An engine for searchable, web-ready historical patterns.

## Getting Started
1. Install dependencies: `pip install -r requirements.txt`
2. Start the server: `python main.py`
3. Access the API at `http://localhost:5000`
